{
  "name": "@atlas/common",
  "version": "0.0.1",
  "description": "Common logic and types between TS server and client",
  "types": "target/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wadejensen/atlas-v2.git"
  },
  "author": "wadejensen",
  "license": "UNLICENSED",
  "scripts": {
    "build:watch": "tsc --watch",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {},
  "dependencies": {
    "@types/express": "^4.16.1",
    "typescript": "3.4.2"
  },
  "engines": {
    "node": ">=10.7.0 < 11",
    "yarn": ">=1.12.3 < 2 || >=1.7.0 < 1.10"
  }
}
